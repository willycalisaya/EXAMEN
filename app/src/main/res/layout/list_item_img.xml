<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <?xml version="1.0" encoding="utf-8"?>
    <ImageView
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/local_img"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:src="@tools:sample/backgrounds/scenic" />
    <!--
    1. Esta vista muestra una imagen ocupando toda la pantalla.
    2. Se utiliza `tools:src` para mostrar una imagen de muestra solo en el editor (no en tiempo de ejecución).
    3. Ideal para vistas donde se desea mostrar imágenes locales o cargadas dinámicamente.
    4. Cambiar `match_parent` a valores específicos si se necesita un tamaño fijo.
    -->


</androidx.constraintlayout.widget.ConstraintLayout>